<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="420" Width="560" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Border CornerRadius="16" Background="White" Padding="40" Width="360" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="#E1E5EA" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Добро пожаловать" FontSize="24" FontWeight="Bold" Foreground="#1F1F1F" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="Войдите в систему, чтобы продолжить" FontSize="13" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,0,0,24"/>

                <TextBlock Text="Логин" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                <TextBox x:Name="tbLogin" Margin="0,0,0,16"/>

                <TextBlock Text="Пароль" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                <PasswordBox x:Name="pbPassword" Margin="0,0,0,6"/>

                <Button Content="Войти" Click="BtnLogin_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
